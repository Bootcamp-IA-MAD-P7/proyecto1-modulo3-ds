# F5 RiskAI — variables de entorno del frontend (copia este archivo a .env dentro de frontend/).
# URL base de la API backend. En desarrollo local: el uvicorn en http://127.0.0.1:8000.
# En Render: la URL del servicio web (https://tu-servicio.onrender.com).
VITE_API_URL=http://127.0.0.1:8000